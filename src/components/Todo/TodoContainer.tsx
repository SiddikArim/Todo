import Container from "../ui/Container";
import TodoCard from "./TodoCard";

const TodoContainer = () => {
  return (
    <Container>
      <TodoCard />
    </Container>
  );
};

export default TodoContainer;
